This repository currently deploys to Azure App Service through `main_financepro.yml`.

The older Azure Static Web Apps workflow was removed because the active Azure resource is an App Service Web App.
